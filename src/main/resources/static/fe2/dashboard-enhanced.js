const API_BASE = "/api"; const state = {   currentView: "screen",   aiSessionId: "",   selectedDeviceId: "",   selectedDeviceIds: [],   selectedAlarmIds: [],   screen: { stats: {}, realtime: {}, alarmSample: [] },   analysis: { alarmTrend: [], alarmSample: [], deviceStats: [] },   devicesPage: { page: 1, pageSize: 10, total: 0, pages: 1, records: [] },   alarmsPage: { page: 1, pageSize: 10, total: 0, pages: 1, records: [] }, };  const charts = {   screenRealtime: null,   screenDeviceStatus: null,   screenAlarmType: null,   screenHeatmap: null,   screenTrend: null,   screenBuilding: null,   analysisTrend: null,   analysisAlarmType: null,   analysisDeviceStatus: null,   analysisBuilding: null, };  function el(id) { return document.getElementById(id); } function safeText(value, fallback = "--") { return value === null || value === undefined || value === "" ? fallback : String(value); } function escapeHtml(value) { return String(value ?? "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/\"/g, "&quot;").replace(/'/g, "&#39;"); } function buildSessionId() { return "sess-" + Date.now().toString(36) + "-" + Math.random().toString(36).slice(2, 8); } function getToken() { return localStorage.getItem("smoke_token") || ""; }
function renderUserInfo() {
  var n = document.getElementById("userDisplay");
  if (!n) return;
  try {
    var u = JSON.parse(localStorage.getItem("smoke_user") || "{}");
    if (u.realName || u.username) {
      var r = { SYSTEM_ADMIN: "系统管理员", COMMUNITY_ADMIN: "小区管理员", FIREFIGHTER: "消防员", RESIDENT: "居民" };
      n.innerHTML = "<strong>" + (u.realName || u.username) + "</strong>" + (u.role ? " <span style=\"color:var(--muted);font-size:12px\">(" + (r[u.role] || u.role) + ")</span>" : "");
    } else { n.textContent = "未登录"; }
  } catch(e) { n.textContent = "--"; }
}
function handleLogout() {
  fetch("/api/auth/logout",{method:"POST",headers:{"Authorization":"Bearer "+getToken()}}).catch(function(){}).finally(function(){localStorage.removeItem("smoke_token");localStorage.removeItem("smoke_user");window.location.href="/";});
}  async function apiRequest(path, options = {}) {   const headers = { "Content-Type": "application/json", ...(options.headers || {}) };   const token = getToken();   if (token) headers.Authorization = "Bearer " + token;   const response = await fetch(API_BASE + path, { ...options, headers }); if (response.status === 401) { localStorage.removeItem("smoke_token"); localStorage.removeItem("smoke_user"); window.location.href = "/"; throw new Error("未登录"); }  if (!response.ok) throw new Error("HTTP " + response.status + " " + path);   const body = await response.json();   if (body && typeof body === "object" && Object.prototype.hasOwnProperty.call(body, "code")) {     if (body.code !== 200) throw new Error(body.msg || body.message || "接口返回失败");     return body.data;   }   return body; }  function setClock() {   const node = el("clock");   if (node) node.textContent = new Date().toLocaleString("zh-CN", { hour12: false }); } function setChip(id, text, level) {   const node = el(id);   if (!node) return;   node.textContent = text;   node.classList.remove("ok", "warn", "danger");   if (level) node.classList.add(level); } function setSyncTime() {   const now = new Date().toLocaleString("zh-CN", { hour12: false });   setChip("systemLastSync", "最近同步: " + now);   const footer = el("footerSyncTime");   if (footer) footer.textContent = "最后同步: " + now; }  function normalizePageResult(payload, fallbackPage = 1, fallbackPageSize = 10) {   const source = payload && typeof payload === "object" ? payload : {};   const records = Array.isArray(source.records) ? source.records : Array.isArray(source.list) ? source.list : Array.isArray(source) ? source : [];   const page = Number(source.page || source.current || fallbackPage || 1);   const pageSize = Number(source.pageSize || fallbackPageSize || records.length || 10);   const total = Number(source.total || records.length || 0);   const pages = Number(source.pages || Math.max(1, Math.ceil(total / Math.max(pageSize, 1))));   return { page, pageSize, total, pages, records }; } function alarmStatusClass(status) {   const s = String(status || "").toUpperCase();   if (s === "PENDING" || s === "CONFIRMING") return "warn";   if (s === "RESOLVED" || s === "ARCHIVED" || s === "CLOSED") return "ok";   if (s === "CONFIRMED") return "info";   return "info"; } function alarmLevelClass(level) {   const s = String(level || "").toUpperCase();   if (s === "HIGH" || s === "CRITICAL") return "danger";   if (s === "MEDIUM") return "warn";   return "info"; } function deviceStatusClass(status) {   const s = String(status || "").toUpperCase();   if (s === "ONLINE") return "ok";   if (s === "OFFLINE") return "warn";   if (s === "ERROR") return "danger";   return "info"; } function renderEmptyState(node, title, desc) {   if (!node) return;   node.innerHTML = '<div class="empty-state"><strong>' + escapeHtml(title) + '</strong><p>' + escapeHtml(desc) + '</p></div>'; } function chartPalette(index) { return ["#2563eb", "#f97316", "#059669", "#dc2626", "#7c3aed", "#0ea5e9"][index % 6]; } function renderFallbackLegend(series) { return '<div style="display:flex;gap:12px;flex-wrap:wrap;justify-content:flex-end;margin-bottom:10px;">' + series.map((item, index) => '<span style="display:inline-flex;align-items:center;gap:6px;color:#64748b;font-size:12px;"><i style="display:inline-block;width:10px;height:10px;border-radius:999px;background:' + chartPalette(index) + ';"></i>' + escapeHtml(safeText(item.name, 'Series ' + (index + 1))) + '</span>').join('') + '</div>'; } function renderFallbackCartesian(node, option, type) {   const series = (option.series || []).filter((item) => Array.isArray(item.data));   const labels = (option.xAxis && option.xAxis.data) || [];   const values = [];   const formatLabel = (label, total) => {     const raw = String(label == null ? '' : label).trim();     if (!raw) return '--';     const normalized = raw.replace('T', ' ');     const date = new Date(normalized);     if (!Number.isNaN(date.getTime())) {       const mm = String(date.getMonth() + 1).padStart(2, '0');       const dd = String(date.getDate()).padStart(2, '0');       const hh = String(date.getHours()).padStart(2, '0');       const mi = String(date.getMinutes()).padStart(2, '0');       return total > 12 ? hh + ':' + mi : mm + '-' + dd + ' ' + hh + ':' + mi;     }     return raw.length > 10 ? raw.slice(0, 10) : raw;   };   series.forEach((item) => item.data.forEach((value) => values.push(Number(value || 0))));   const max = Math.max(1, ...values);   const width = 640; const height = 300; const left = 46; const right = 18; const top = 18; const bottom = 66;   const plotWidth = width - left - right; const plotHeight = height - top - bottom;   const slot = labels.length ? plotWidth / labels.length : plotWidth;   const tickLimit = labels.length > 18 ? 5 : labels.length > 10 ? 6 : Math.max(labels.length, 1);   const tickStep = Math.max(1, Math.ceil(labels.length / tickLimit));   const showMarkers = type !== 'bar' && labels.length <= 12;   let body = '';   if (type === 'bar') {     const barSeries = series.length || 1;     series.forEach((item, seriesIndex) => {       const barWidth = Math.max(8, Math.min(24, slot / Math.max(barSeries + 0.8, 1.8)));       item.data.forEach((raw, index) => {         const value = Number(raw || 0);         const h = (value / max) * plotHeight;         const x = left + slot * index + (slot - barWidth * barSeries) / 2 + seriesIndex * barWidth;         const y = top + plotHeight - h;         body += '<rect x="' + x.toFixed(2) + '" y="' + y.toFixed(2) + '" width="' + barWidth.toFixed(2) + '" height="' + h.toFixed(2) + '" rx="7" fill="' + chartPalette(seriesIndex) + '" opacity="0.88"></rect>';       });     });   } else {     series.forEach((item, seriesIndex) => {       const points = item.data.map((raw, index) => {         const value = Number(raw || 0);         const x = left + slot * index + slot / 2;         const y = top + plotHeight - (value / max) * plotHeight;         return x.toFixed(2) + ',' + y.toFixed(2);       }).join(' ');       body += '<polyline fill="none" stroke="' + chartPalette(seriesIndex) + '" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" points="' + points + '"></polyline>';       if (showMarkers) {         item.data.forEach((raw, index) => {           const value = Number(raw || 0);           const x = left + slot * index + slot / 2;           const y = top + plotHeight - (value / max) * plotHeight;           body += '<circle cx="' + x.toFixed(2) + '" cy="' + y.toFixed(2) + '" r="3.6" fill="' + chartPalette(seriesIndex) + '" stroke="#ffffff" stroke-width="1.5"></circle>';         });       }     });   }   const grid = [0, 0.25, 0.5, 0.75, 1].map((rate) => { const y = top + plotHeight - rate * plotHeight; const value = (max * rate).toFixed(0); return '<line x1="' + left + '" y1="' + y.toFixed(2) + '" x2="' + (width - right) + '" y2="' + y.toFixed(2) + '" stroke="rgba(148,163,184,0.18)"></line><text x="' + (left - 8) + '" y="' + (y + 4).toFixed(2) + '" text-anchor="end" fill="#94a3b8" font-size="11">' + value + '</text>'; }).join('');   const axis = '<line x1="' + left + '" y1="' + top + '" x2="' + left + '" y2="' + (top + plotHeight) + '" stroke="#cbd5e1"></line><line x1="' + left + '" y1="' + (top + plotHeight) + '" x2="' + (width - right) + '" y2="' + (top + plotHeight) + '" stroke="#cbd5e1"></line>';   const ticks = labels.map((label, index) => {     const isLast = index === labels.length - 1;     if (index % tickStep !== 0 && !isLast) return '';     const x = left + slot * index + slot / 2;     const text = formatLabel(label, labels.length);     const row = Math.floor(index / tickStep) % 2;     const y = height - (row ? 10 : 24);     return '<text x="' + x.toFixed(2) + '" y="' + y + '" text-anchor="middle" fill="#94a3b8" font-size="11">' + escapeHtml(text) + '</text>';   }).join('');   node.innerHTML = '<div style="height:100%;display:flex;flex-direction:column;justify-content:center;">' + renderFallbackLegend(series) + '<svg viewBox="0 0 ' + width + ' ' + height + '" width="100%" height="100%" preserveAspectRatio="none">' + grid + axis + body + ticks + '</svg></div>'; } function renderFallbackPie(node, option) {   const data = (((option.series || [])[0] || {}).data || []).filter((item) => Number(item.value || 0) > 0);   const total = data.reduce((sum, item) => sum + Number(item.value || 0), 0) || 1;   let acc = 0;   const segments = data.map((item, index) => { const start = (acc / total) * 360; acc += Number(item.value || 0); const end = (acc / total) * 360; return chartPalette(index) + ' ' + start.toFixed(2) + 'deg ' + end.toFixed(2) + 'deg'; }).join(', ');   const legend = data.map((item, index) => '<div style="display:flex;align-items:center;justify-content:space-between;gap:12px;padding:6px 0;border-bottom:1px solid rgba(148,163,184,0.12);"><span style="display:inline-flex;align-items:center;gap:8px;color:#475569;font-size:13px;"><i style="display:inline-block;width:10px;height:10px;border-radius:999px;background:' + chartPalette(index) + ';"></i>' + escapeHtml(safeText(item.name, 'Other')) + '</span><strong style="color:#1e293b;font-size:13px;">' + escapeHtml(String(item.value)) + '</strong></div>').join('');   node.innerHTML = '<div style="height:100%;display:grid;grid-template-columns:minmax(180px,220px) 1fr;align-items:center;gap:24px;padding:8px 0;">' + '<div style="display:grid;place-items:center;">' + '<div style="width:180px;height:180px;border-radius:999px;background:conic-gradient(' + segments + ');position:relative;box-shadow:inset 0 0 0 1px rgba(148,163,184,0.14);">' + '<div style="position:absolute;inset:28px;border-radius:999px;background:#fff;display:grid;place-items:center;text-align:center;">' + '<div><strong style="display:block;font-size:26px;color:#1e293b;">' + total + '</strong><span style="font-size:12px;color:#94a3b8;">Total</span></div>' + '</div></div></div>' + '<div style="display:grid;align-content:center;gap:4px;">' + legend + '</div></div>'; } function renderFallbackHeatmap(node, option) {   const xLabels = (option.xAxis && option.xAxis.data) || [];   const yLabels = (option.yAxis && option.yAxis.data) || [];   const data = (((option.series || [])[0] || {}).data || []);   const max = Math.max(1, ...data.map((item) => Number((item || [])[2] || 0)));   const map = new Map();   const shortLabel = (label) => { const text = String(label == null ? '' : label); return text.length > 8 ? text.slice(0, 8) : text; };   data.forEach((item) => map.set(item[0] + '-' + item[1], Number(item[2] || 0)));   const gridCols = Math.max(1, xLabels.length);   const gridRows = Math.max(1, yLabels.length);   const compactMode = gridCols * gridRows <= 4;   const cellSize = compactMode ? 76 : gridCols >= 5 ? 52 : 60;   const cells = [];   for (let y = yLabels.length - 1; y >= 0; y -= 1) {     for (let x = 0; x < xLabels.length; x += 1) {       const value = map.get(x + '-' + y) || 0;       const rate = value / max;       const alpha = value === 0 ? 0.1 : 0.22 + rate * 0.72;       const color = value > max * 0.66 ? '220,38,38' : value > max * 0.33 ? '249,115,22' : '37,99,235';       cells.push('<div style="height:' + cellSize + 'px;min-width:' + cellSize + 'px;border-radius:' + (compactMode ? 16 : 12) + 'px;background:linear-gradient(180deg, rgba(' + color + ',' + Math.min(alpha + 0.12, 0.96).toFixed(2) + '), rgba(' + color + ',' + alpha.toFixed(2) + '));border:1px solid rgba(255,255,255,0.65);box-shadow:inset 0 1px 0 rgba(255,255,255,0.22);display:grid;place-items:center;color:' + (value === 0 ? '#64748b' : '#ffffff') + ';font-size:' + (compactMode ? 16 : 13) + 'px;font-weight:700;">' + value + '</div>');     }   }   const cols = '88px repeat(' + gridCols + ', minmax(' + cellSize + 'px, ' + cellSize + 'px))';   const rows = yLabels.slice().reverse().map((label, rowIndex) => '<div style="display:contents;"><div style="display:grid;place-items:center;justify-content:end;padding-right:8px;color:#64748b;font-size:12px;font-weight:600;">' + escapeHtml(shortLabel(label)) + '</div>' + cells.slice(rowIndex * gridCols, (rowIndex + 1) * gridCols).join('') + '</div>').join('');   const header = '<div style="display:contents;"><div></div>' + xLabels.map((label) => '<div style="display:grid;place-items:center;color:#64748b;font-size:12px;font-weight:600;padding-bottom:6px;">' + escapeHtml(shortLabel(label)) + '</div>').join('') + '</div>';   const legend = '<div style="display:flex;justify-content:center;gap:14px;flex-wrap:wrap;margin-top:14px;color:#64748b;font-size:12px;"><span style="display:inline-flex;align-items:center;gap:6px;"><i style="display:inline-block;width:12px;height:12px;border-radius:999px;background:rgba(37,99,235,0.78);"></i>Low</span><span style="display:inline-flex;align-items:center;gap:6px;"><i style="display:inline-block;width:12px;height:12px;border-radius:999px;background:rgba(249,115,22,0.82);"></i>Medium</span><span style="display:inline-flex;align-items:center;gap:6px;"><i style="display:inline-block;width:12px;height:12px;border-radius:999px;background:rgba(220,38,38,0.86);"></i>High</span></div>';   node.innerHTML = '<div style="height:100%;display:flex;align-items:center;justify-content:center;padding:8px 0;"><div style="width:min(100%,' + (88 + gridCols * (cellSize + 10)) + 'px);display:flex;flex-direction:column;align-items:center;"><div style="display:grid;grid-template-columns:' + cols + ';gap:10px;align-content:center;">' + header + rows + '</div>' + legend + '</div></div>'; } function disposeChart(key) { if (charts[key]) { charts[key].dispose(); charts[key] = null; } } function ensureChart(key, nodeId) {   const node = el(nodeId);   if (!node || typeof echarts === "undefined") return null;   if (!charts[key]) charts[key] = echarts.init(node);   return charts[key]; } function renderChart(key, nodeId, option, hasData, emptyTitle, emptyDesc) {   const node = el(nodeId);   if (!node) return;   if (!hasData) { disposeChart(key); renderEmptyState(node, emptyTitle, emptyDesc); return; }   if (typeof echarts === "undefined") {     const series = Array.isArray(option && option.series) ? option.series : [];     const chartType = series[0] && series[0].type;     if (chartType === "pie") renderFallbackPie(node, option);     else if (chartType === "heatmap") renderFallbackHeatmap(node, option);     else renderFallbackCartesian(node, option, chartType === "bar" ? "bar" : "line");     return;   }   node.innerHTML = "";   const chart = ensureChart(key, nodeId);   if (chart) chart.setOption(option, true); } function resizeVisibleCharts() { Object.values(charts).forEach((chart) => { if (chart && typeof chart.resize === "function") chart.resize(); }); } function getActiveAlarm() {   const realtime = state.screen.realtime || {};   const active = Array.isArray(realtime.activeAlarms) ? realtime.activeAlarms[0] : null;   if (active) return active;   return state.analysis.alarmSample.find((item) => String(item.alarmStatus || "").toUpperCase() === "PENDING") || state.screen.alarmSample[0] || null; }  function setNavState(view) { document.querySelectorAll(".nav-btn").forEach((item) => item.classList.toggle("active", item.dataset.view === view)); } function switchView(view) {   state.currentView = view;   document.querySelectorAll(".view").forEach((node) => node.classList.remove("active"));   const target = el("view-" + view);   if (target) target.classList.add("active");   setNavState(view);   const map = {     screen: ["首页 / 数据大屏", "设备态势、实时监测、告警联动"],     devices: ["设备管理", "设备状态、关键参数与运行信息"],     analysis: ["数据分析", "趋势分析、类型占比与楼栋分布"],     ai: ["AI 智能问答", "知识问答与火情研判"],     alarms: ["告警日志", "告警记录、确认和处置流程"],   };   const pair = map[view] || map.screen;   const title = el("viewTitle");   const subtitle = el("viewSubTitle");   const banner = el("bannerTitle");   if (title) title.textContent = pair[0];   if (subtitle) subtitle.textContent = pair[1];   if (banner) banner.textContent = pair[0];   setTimeout(resizeVisibleCharts, 80); } function initMenus() {   document.querySelectorAll(".nav-btn").forEach((btn) => btn.addEventListener("click", () => switchView(btn.dataset.view)));   const openAlarms = el("btnOpenAlarms");   if (openAlarms) openAlarms.addEventListener("click", () => switchView("alarms")); }  function renderScreenKpi() {   const stats = state.screen.stats || {};   const total = Number(stats.totalDevices || 0);   const online = Number(stats.onlineDevices || 0);   const today = Number(stats.todayAlarms || 0);   const pending = Number(stats.pendingAlarms || 0);   const onlineRate = total > 0 ? ((online / total) * 100).toFixed(1) : "0.0";   el("kpiTotalDevices").textContent = String(total);   el("kpiOnlineDevices").textContent = String(online);   el("kpiTodayAlarms").textContent = String(today);   el("kpiPendingAlarms").textContent = String(pending);   const onlineRateNode = el("screenOnlineRate");   if (onlineRateNode) onlineRateNode.textContent = onlineRate;   setChip("activeAlarmStatus", "活跃告警: " + String(pending), pending > 0 ? "warn" : "ok"); } function renderLatestMetrics() {   const realtime = state.screen.realtime || {};   const latestList = Array.isArray(realtime.latestData) ? realtime.latestData : [];   const latest = latestList.length ? latestList[latestList.length - 1] : null;   const smoke = Number(latest && (latest.smoke || latest.smokeValue || latest.smokeConcentration) || 0);   const temp = Number(latest && (latest.temperature || latest.tempValue) || 0);   const signal = Number(latest && (latest.signalStrength || latest.rssi) || 0);   const smokeNode = el("screenSmokeValue");   const tempNode = el("screenTempValue");   const signalNode = el("screenSignalValue");   if (smokeNode) smokeNode.textContent = latest ? smoke.toFixed(1) : "--";   if (tempNode) tempNode.textContent = latest ? temp.toFixed(1) : "--";   if (signalNode) signalNode.textContent = latest && Number.isFinite(signal) ? String(signal) : "--"; } function getScreenDeviceStatusData() {   const realtime = state.screen.realtime || {};   const map = realtime.deviceStatusMap || {};   const stats = state.screen.stats || {};   const source = {     ONLINE: Number(map.ONLINE || stats.onlineDevices || 0),     OFFLINE: Number(map.OFFLINE || stats.offlineDevices || 0),     ERROR: Number(map.ERROR || stats.errorDevices || 0),     INACTIVE: Number(map.INACTIVE || 0),   };   return Object.keys(source).map((name) => ({ name, value: source[name] })); } function buildAlarmTypeSeries(rows) {   const counts = {};   (rows || []).forEach((item) => { const name = safeText(item.alarmType || item.type || item.alarmName || "未分类"); counts[name] = (counts[name] || 0) + 1; });   return Object.keys(counts).map((name) => ({ name, value: counts[name] })); } function buildHeatmapData(points, alarms) {   const buildingMap = new Map();   const floorMap = new Map();   const matrix = new Map();   const add = (building, floor, weight) => {     const b = safeText(building, "未分类楼栋");     const f = safeText(floor, "未分类楼层");     if (!buildingMap.has(b)) buildingMap.set(b, buildingMap.size);     if (!floorMap.has(f)) floorMap.set(f, floorMap.size);     const key = b + "::" + f;     matrix.set(key, (matrix.get(key) || 0) + Number(weight || 1));   };   (points || []).forEach((item) => add(item.locationBuilding || item.building, item.locationFloor || item.floor, 1));   (alarms || []).forEach((item) => add(item.locationBuilding || item.building, item.locationFloor || item.floor, 2));   const buildings = Array.from(buildingMap.keys());   const floors = Array.from(floorMap.keys());   const data = [];   matrix.forEach((value, key) => { const pair = key.split("::"); data.push([buildingMap.get(pair[0]), floorMap.get(pair[1]), value]); });   return { buildings, floors, data }; } function renderScreenAlarmList() {   const list = el("screenAlarmList");   const rows = (state.screen.alarmSample || []).slice(0, 8);   if (!rows.length) { renderEmptyState(list, "暂无告警", "当前没有可展示的活跃告警数据。请稍后刷新或检查后端数据。"); return; }   list.innerHTML = rows.map((item) => {     const levelClass = alarmLevelClass(item.alarmLevel);     return '<li class="list-item alarm-card ' + levelClass + '"><div class="card-row"><strong>' + escapeHtml(safeText(item.alarmType, "告警")) + '</strong><span class="status-badge ' + alarmStatusClass(item.alarmStatus) + '">' + escapeHtml(safeText(item.alarmStatus, "--")) + '</span></div><div style="margin-top:6px;color:#64748b;">设备: ' + escapeHtml(safeText(item.deviceId, "--")) + ' · 楼栋: ' + escapeHtml(safeText(item.locationBuilding || item.building, "--")) + '</div></li>';   }).join(""); } function renderScreenCharts() {   const realtime = state.screen.realtime || {};   const latestData = Array.isArray(realtime.latestData) ? realtime.latestData : [];   const xAxis = latestData.map((item, index) => safeText(item.createTime || item.time || item.timestamp || index + 1, ""));   const smokeSeries = latestData.map((item) => Number(item.smoke || item.smokeValue || item.smokeConcentration || 0));   const tempSeries = latestData.map((item) => Number(item.temperature || item.tempValue || 0));   renderChart("screenRealtime", "chartRealtime", {     tooltip: { trigger: "axis" }, legend: { top: 0, textStyle: { color: "#475569" } }, grid: { left: 44, right: 20, top: 34, bottom: 30 },     xAxis: { type: "category", data: xAxis, axisLabel: { color: "#64748b" }, axisLine: { lineStyle: { color: "#cbd5e1" } } },     yAxis: { type: "value", axisLabel: { color: "#64748b" }, splitLine: { lineStyle: { color: "rgba(148,163,184,0.18)" } } },     series: [       { name: "烟雾", type: "line", smooth: true, data: smokeSeries, itemStyle: { color: "#2563eb" }, areaStyle: { color: "rgba(37,99,235,0.12)" } },       { name: "温度", type: "line", smooth: true, data: tempSeries, itemStyle: { color: "#f97316" }, areaStyle: { color: "rgba(249,115,22,0.08)" } },     ],   }, latestData.length > 0, "暂无趋势数据", "当前没有可用的实时传感器数据，稍后刷新或检查设备在线状态。");    const deviceStatusData = getScreenDeviceStatusData();   renderChart("screenDeviceStatus", "chartScreenDeviceStatus", {     tooltip: { trigger: "item" }, legend: { bottom: 0, textStyle: { color: "#475569" } },     series: [{ type: "pie", radius: ["45%", "72%"], center: ["50%", "48%"], avoidLabelOverlap: false, label: { color: "#334155" }, data: deviceStatusData, itemStyle: { borderColor: "#fff", borderWidth: 2 } }],   }, deviceStatusData.some((item) => Number(item.value) > 0), "暂无设备状态", "设备在线状态暂无统计数据，请先刷新后端接口或检查设备统计是否返回。");    const alarmTypeData = buildAlarmTypeSeries(state.screen.alarmSample);   renderChart("screenAlarmType", "chartScreenAlarmType", {     tooltip: { trigger: "item" }, legend: { bottom: 0, textStyle: { color: "#475569" } },     series: [{ type: "pie", radius: ["42%", "70%"], center: ["50%", "48%"], data: alarmTypeData, itemStyle: { borderColor: "#fff", borderWidth: 2 }, label: { color: "#334155" } }],   }, alarmTypeData.some((item) => Number(item.value) > 0), "暂无类型占比", "当前没有足够的告警样本用于生成告警类型占比图。");    const heatmap = buildHeatmapData(latestData, state.screen.alarmSample);   renderChart("screenHeatmap", "chartHeatmap", {     tooltip: { trigger: "item", formatter: (params) => {       const value = params.value || [];       return escapeHtml(safeText(heatmap.buildings[value[0]], "未知楼栋")) + " / " + escapeHtml(safeText(heatmap.floors[value[1]], "未知楼层")) + "<br/>热度: " + escapeHtml(safeText(value[2], 0));     } },     grid: { left: 48, right: 18, top: 20, bottom: 52 },     xAxis: { type: "category", data: heatmap.buildings, axisLabel: { color: "#64748b" }, axisLine: { lineStyle: { color: "#cbd5e1" } } },     yAxis: { type: "category", data: heatmap.floors, axisLabel: { color: "#64748b" }, axisLine: { lineStyle: { color: "#cbd5e1" } } },     visualMap: { min: 0, max: Math.max(5, ...heatmap.data.map((item) => Number(item[2] || 0))), orient: "horizontal", left: "center", bottom: 4, textStyle: { color: "#64748b" }, inRange: { color: ["#eff6ff", "#bfdbfe", "#60a5fa", "#f97316", "#ef4444"] } },     series: [{ type: "heatmap", data: heatmap.data, label: { show: false } }],   }, heatmap.data.length > 0, "暂无热力分布", "当前没有可用的楼栋与楼层维度数据。");    const sevenDayTrend = Array.isArray(state.analysis.alarmTrend) ? state.analysis.alarmTrend : [];   renderChart("screenTrend", "chartScreenTrend", {     tooltip: { trigger: "axis" }, grid: { left: 42, right: 18, top: 28, bottom: 34 },     xAxis: { type: "category", data: sevenDayTrend.map((item) => item.date), axisLabel: { color: "#64748b" }, axisLine: { lineStyle: { color: "#cbd5e1" } } },     yAxis: { type: "value", axisLabel: { color: "#64748b" }, splitLine: { lineStyle: { color: "rgba(148,163,184,0.18)" } } },     series: [{ type: "bar", data: sevenDayTrend.map((item) => Number(item.total || 0)), itemStyle: { color: "#2563eb" } }],   }, sevenDayTrend.length > 0, "暂无七日趋势", "告警趋势数据暂不可用，请先加载数据分析接口。");    const buildingData = Array.isArray(state.analysis.deviceStats) ? state.analysis.deviceStats.map((item) => ({ name: safeText(item.building, "未分类"), value: Number(item.total || 0) })) : [];   renderChart("screenBuilding", "chartScreenBuilding", {     tooltip: { trigger: "axis" }, grid: { left: 42, right: 18, top: 28, bottom: 36 },     xAxis: { type: "category", data: buildingData.map((item) => item.name), axisLabel: { color: "#64748b" }, axisLine: { lineStyle: { color: "#cbd5e1" } } },     yAxis: { type: "value", axisLabel: { color: "#64748b" }, splitLine: { lineStyle: { color: "rgba(148,163,184,0.18)" } } },     series: [{ type: "bar", data: buildingData.map((item) => item.value), itemStyle: { color: "#f97316" } }],   }, buildingData.some((item) => Number(item.value) > 0), "暂无楼栋分布", "设备楼栋分布暂不可用，请先加载数据分析接口。"); } function renderAnalysisCharts() {   const trend = state.analysis.alarmTrend || [];   renderChart("analysisTrend", "chartAlarmTrend", {     tooltip: { trigger: "axis" }, legend: { top: 0, textStyle: { color: "#475569" } }, grid: { left: 44, right: 20, top: 34, bottom: 32 },     xAxis: { type: "category", data: trend.map((item) => item.date), axisLabel: { color: "#64748b" }, axisLine: { lineStyle: { color: "#cbd5e1" } } },     yAxis: { type: "value", axisLabel: { color: "#64748b" }, splitLine: { lineStyle: { color: "rgba(148,163,184,0.18)" } } },     series: [       { name: "总告警", type: "bar", data: trend.map((item) => Number(item.total || 0)), itemStyle: { color: "#2563eb" } },       { name: "烟雾超标", type: "line", smooth: true, data: trend.map((item) => Number(item.smokeOverflow || 0)), itemStyle: { color: "#f97316" } },       { name: "设备离线", type: "line", smooth: true, data: trend.map((item) => Number(item.deviceOffline || 0)), itemStyle: { color: "#ef4444" } },     ],   }, trend.length > 0, "暂无趋势数据", "告警趋势接口尚未返回有效数据，请检查 dashboard/alarm-stats。");   const alarmTypes = buildAlarmTypeSeries(state.analysis.alarmSample);   renderChart("analysisAlarmType", "chartAlarmType", {     tooltip: { trigger: "item" }, legend: { bottom: 0, textStyle: { color: "#475569" } },     series: [{ type: "pie", radius: ["42%", "70%"], center: ["50%", "48%"], data: alarmTypes, label: { color: "#334155" }, itemStyle: { borderColor: "#fff", borderWidth: 2 } }],   }, alarmTypes.some((item) => Number(item.value) > 0), "暂无类型占比", "告警样本不足，无法生成类型占比图。");   const deviceTotals = { ONLINE: 0, OFFLINE: 0, ERROR: 0, INACTIVE: 0 };   (state.analysis.deviceStats || []).forEach((item) => { deviceTotals.ONLINE += Number(item.online || 0); deviceTotals.OFFLINE += Number(item.offline || 0); deviceTotals.ERROR += Number(item.error || 0); deviceTotals.INACTIVE += Number(item.inactive || 0); });   const deviceStatusData = Object.keys(deviceTotals).map((name) => ({ name, value: deviceTotals[name] }));   renderChart("analysisDeviceStatus", "chartDeviceStatus", {     tooltip: { trigger: "item" }, legend: { bottom: 0, textStyle: { color: "#475569" } },     series: [{ type: "pie", radius: ["42%", "70%"], center: ["50%", "48%"], data: deviceStatusData, label: { color: "#334155" }, itemStyle: { borderColor: "#fff", borderWidth: 2 } }],   }, deviceStatusData.some((item) => Number(item.value) > 0), "暂无设备状态", "设备状态统计为空，请检查 dashboard/device-stats。");   const buildingData = (state.analysis.deviceStats || []).map((item) => ({ name: safeText(item.building, "未分类"), value: Number(item.total || 0) }));   renderChart("analysisBuilding", "chartDeviceBuilding", {     tooltip: { trigger: "axis" }, grid: { left: 42, right: 20, top: 32, bottom: 36 },     xAxis: { type: "category", data: buildingData.map((item) => item.name), axisLabel: { color: "#64748b" }, axisLine: { lineStyle: { color: "#cbd5e1" } } },     yAxis: { type: "value", axisLabel: { color: "#64748b" }, splitLine: { lineStyle: { color: "rgba(148,163,184,0.18)" } } },     series: [{ type: "bar", data: buildingData.map((item) => item.value), itemStyle: { color: "#0ea5e9" } }],   }, buildingData.some((item) => Number(item.value) > 0), "暂无楼栋分布", "设备楼栋分布为空，请检查 dashboard/device-stats。"); }  function updateScreenDeviceSelect() {   const select = el("screenDeviceSelect");   if (!select) return;   const records = state.devicesPage.records || [];   select.innerHTML = records.map((item) => '<option value="' + escapeHtml(safeText(item.id, "")) + '">' + escapeHtml(safeText(item.deviceName, item.deviceCode || "设备")) + '</option>').join("") || '<option value="">暂无设备</option>';   if (!state.selectedDeviceId && records.length) state.selectedDeviceId = String(records[0].id || "");   if (state.selectedDeviceId) select.value = state.selectedDeviceId; } function updateDeviceBatchHint() {   const node = el("deviceBatchHint"); if (node) node.textContent = "已选择 " + state.selectedDeviceIds.length + " 台设备";   const selectAll = el("deviceSelectAll"); if (selectAll) selectAll.checked = state.devicesPage.records.length > 0 && state.selectedDeviceIds.length === state.devicesPage.records.length; } function updateAlarmBatchHint() {   const node = el("alarmBatchHint"); if (node) node.textContent = "已选择 " + state.selectedAlarmIds.length + " 条告警";   const selectAll = el("alarmSelectAll"); if (selectAll) selectAll.checked = state.alarmsPage.records.length > 0 && state.selectedAlarmIds.length === state.alarmsPage.records.length; } function toggleDeviceSelection(id, checked) {   const value = String(id);   state.selectedDeviceIds = checked ? Array.from(new Set([...state.selectedDeviceIds, value])) : state.selectedDeviceIds.filter((item) => item !== value);   updateDeviceBatchHint(); } function toggleAlarmSelection(id, checked) {   const value = String(id);   state.selectedAlarmIds = checked ? Array.from(new Set([...state.selectedAlarmIds, value])) : state.selectedAlarmIds.filter((item) => item !== value);   updateAlarmBatchHint(); }  function openDetailModal(title, rows) {   const modal = el("detailModal");   const heading = el("detailModalTitle");   const body = el("detailModalBody");   if (!modal || !heading || !body) return;   heading.textContent = title;   body.innerHTML = '<div class="detail-grid">' + rows.map((item) => item.full ? '<div class="detail-item detail-full"><span>' + escapeHtml(item.label) + '</span><strong>' + escapeHtml(item.value) + '</strong></div>' : '<div class="detail-item"><span>' + escapeHtml(item.label) + '</span><strong>' + escapeHtml(item.value) + '</strong></div>').join("") + '</div>';   modal.classList.remove("hidden"); } function closeDetailModal() { const modal = el("detailModal"); if (modal) modal.classList.add("hidden"); } function showDeviceDetail(id) {   const item = (state.devicesPage.records || []).find((row) => String(row.id) === String(id));   if (!item) return;   openDetailModal("设备详情", [     { label: "设备名称", value: safeText(item.deviceName, "--") },     { label: "设备编号", value: safeText(item.deviceCode || item.deviceId, "--") },     { label: "楼栋", value: safeText(item.locationBuilding || item.building, "--") },     { label: "楼层", value: safeText(item.locationFloor || item.floor, "--") },     { label: "状态", value: safeText(item.status, "--") },     { label: "烟雾", value: safeText(item.latestSmoke, "--") },     { label: "温度", value: safeText(item.latestTemp, "--") },     { label: "湿度", value: safeText(item.latestHumidity, "--") },     { label: "备注", value: safeText(item.remark || item.description, "--"), full: true },   ]); } function showAlarmDetail(id) {   const item = (state.alarmsPage.records || []).find((row) => String(row.id) === String(id));   if (!item) return;   openDetailModal("告警详情", [     { label: "告警类型", value: safeText(item.alarmType, "--") },     { label: "告警等级", value: safeText(item.alarmLevel, "--") },     { label: "告警状态", value: safeText(item.alarmStatus, "--") },     { label: "设备", value: safeText(item.deviceId, "--") },     { label: "楼栋", value: safeText(item.locationBuilding || item.building, "--") },     { label: "时间", value: safeText(item.createTime || item.alarmTime || item.time, "--") },     { label: "告警内容", value: safeText(item.alarmContent || item.content || item.remark, "--"), full: true },   ]); }  function renderDevicesTable() {   const body = el("deviceTableBody");   if (!body) return;   const rows = state.devicesPage.records || [];   if (!rows.length) {     body.innerHTML = '<tr><td colspan="10"><div class="empty-state"><strong>暂无设备</strong><p>当前筛选条件下没有设备记录，请调整筛选条件后重试。</p></div></td></tr>';     updateDeviceBatchHint();     return;   }   body.innerHTML = rows.map((item) => {     const status = String(item.status || "UNKNOWN").toUpperCase();     const checked = state.selectedDeviceIds.includes(String(item.id)) ? 'checked' : '';     return '<tr>' +       '<td class="row-select"><input type="checkbox" data-device-check="true" data-id="' + escapeHtml(safeText(item.id, "")) + '" ' + checked + ' /></td>' +       '<td>' + escapeHtml(safeText(item.deviceName, item.deviceCode || "设备")) + '</td>' +       '<td>' + escapeHtml(safeText(item.deviceCode || item.deviceId, "--")) + '</td>' +       '<td>' + escapeHtml(safeText(item.locationBuilding || item.building, "--")) + '</td>' +       '<td>' + escapeHtml(safeText(item.locationFloor || item.floor, "--")) + '</td>' +       '<td><span class="status-badge ' + deviceStatusClass(status) + '">' + escapeHtml(status) + '</span></td>' +       '<td>' + escapeHtml(safeText(item.latestSmoke, "--")) + '</td>' +       '<td>' + escapeHtml(safeText(item.latestTemp, "--")) + '</td>' +       '<td>' + escapeHtml(safeText(item.latestHumidity, "--")) + '</td>' +       '<td><button class="btn" data-device-detail="true" data-id="' + escapeHtml(safeText(item.id, "")) + '">详情</button></td>' +       '</tr>';   }).join("");   body.querySelectorAll("input[data-device-check]").forEach((input) => input.addEventListener("change", () => toggleDeviceSelection(input.dataset.id, input.checked)));   body.querySelectorAll("button[data-device-detail]").forEach((button) => button.addEventListener("click", () => showDeviceDetail(button.dataset.id)));   updateDeviceBatchHint(); } function renderDevicePagination() {   const node = el("devicePagination");   if (!node) return;   const page = state.devicesPage.page || 1;   const pages = state.devicesPage.pages || 1;   const total = state.devicesPage.total || 0;   node.innerHTML = '<span class="page-info">第 ' + page + ' / ' + pages + ' 页，共 ' + total + ' 条</span><div class="page-actions"><button class="btn" data-page="prev" ' + (page <= 1 ? 'disabled' : '') + '>上一页</button><button class="btn" data-page="next" ' + (page >= pages ? 'disabled' : '') + '>下一页</button></div>';   node.querySelectorAll("button[data-page]").forEach((btn) => btn.addEventListener("click", () => { const targetPage = btn.dataset.page === "next" ? page + 1 : page - 1; loadDevices(targetPage); })); } function buildAlarmActions(item) {   const id = safeText(item.id, "");   const status = String(item.alarmStatus || "").toUpperCase();   const actions = [];   if (status === "PENDING" || status === "CONFIRMING") {     actions.push('<button class="btn btn-main" data-action="confirm" data-id="' + escapeHtml(id) + '">确认</button>');     actions.push('<button class="btn" data-action="resolve" data-id="' + escapeHtml(id) + '">处置</button>');   } else if (status === "CONFIRMED") {     actions.push('<button class="btn btn-main" data-action="resolve" data-id="' + escapeHtml(id) + '">处置</button>');     actions.push('<button class="btn" data-action="archive" data-id="' + escapeHtml(id) + '">归档</button>');   } else if (status === "RESOLVED") {     actions.push('<button class="btn" data-action="archive" data-id="' + escapeHtml(id) + '">归档</button>');   } else if (status !== "ARCHIVED" && status !== "CLOSED") {     actions.push('<button class="btn" data-action="close" data-id="' + escapeHtml(id) + '">关闭</button>');   }   return actions.length ? actions.join("") : '<span class="chip">无可用操作</span>'; } function renderAlarmTable() {   const body = el("alarmTableBody");   if (!body) return;   const rows = state.alarmsPage.records || [];   if (!rows.length) {     body.innerHTML = '<tr><td colspan="9"><div class="empty-state"><strong>暂无告警</strong><p>当前筛选条件下没有告警记录，请更换条件后重试。</p></div></td></tr>';     updateAlarmBatchHint();     return;   }   body.innerHTML = rows.map((item) => {     const status = String(item.alarmStatus || "--");     const level = String(item.alarmLevel || "--");     const actions = buildAlarmActions(item);     const checked = state.selectedAlarmIds.includes(String(item.id)) ? 'checked' : '';     return '<tr>' +       '<td class="row-select"><input type="checkbox" data-alarm-check="true" data-id="' + escapeHtml(safeText(item.id, "")) + '" ' + checked + ' /></td>' +       '<td>' + escapeHtml(safeText(item.createTime || item.alarmTime || item.time, "--")) + '</td>' +       '<td>' + escapeHtml(safeText(item.alarmType, "--")) + '</td>' +       '<td><span class="status-badge ' + alarmLevelClass(level) + '">' + escapeHtml(level) + '</span></td>' +       '<td><span class="status-badge ' + alarmStatusClass(status) + '">' + escapeHtml(status) + '</span></td>' +       '<td>' + escapeHtml(safeText(item.deviceId, "--")) + '</td>' +       '<td>' + escapeHtml(safeText(item.locationBuilding || item.building, "--")) + '</td>' +       '<td><div class="table-actions">' + actions + '</div></td>' +       '<td><button class="btn" data-alarm-detail="true" data-id="' + escapeHtml(safeText(item.id, "")) + '">详情</button></td>' +       '</tr>';   }).join("");   body.querySelectorAll("button[data-action]").forEach((btn) => btn.addEventListener("click", async () => {     try {       await handleAlarmAction(btn.dataset.action, btn.dataset.id);       await loadAlarmRows(state.alarmsPage.page);       await loadScreenData();       await loadAnalysisData();     } catch (error) {       showGlobalAlert("告警操作失败: " + error.message);     }   }));   body.querySelectorAll("input[data-alarm-check]").forEach((input) => input.addEventListener("change", () => toggleAlarmSelection(input.dataset.id, input.checked)));   body.querySelectorAll("button[data-alarm-detail]").forEach((button) => button.addEventListener("click", () => showAlarmDetail(button.dataset.id)));   updateAlarmBatchHint(); } function renderAlarmPagination() {   const node = el("alarmPagination");   if (!node) return;   const page = state.alarmsPage.page || 1;   const pages = state.alarmsPage.pages || 1;   const total = state.alarmsPage.total || 0;   node.innerHTML = '<span class="page-info">第 ' + page + ' / ' + pages + ' 页，共 ' + total + ' 条</span><div class="page-actions"><button class="btn" data-page="prev" ' + (page <= 1 ? 'disabled' : '') + '>上一页</button><button class="btn" data-page="next" ' + (page >= pages ? 'disabled' : '') + '>下一页</button></div>';   node.querySelectorAll("button[data-page]").forEach((btn) => btn.addEventListener("click", () => { const targetPage = btn.dataset.page === "next" ? page + 1 : page - 1; loadAlarmRows(targetPage); })); }  async function batchDeleteDevices() {   if (!state.selectedDeviceIds.length) return showGlobalAlert("请先选择要批量删除的设备");   showGlobalAlert("当前页面只做前端演示批量选择，未调用删除接口以避免越界"); } async function batchHandleAlarms(action) {   if (!state.selectedAlarmIds.length) return showGlobalAlert("请先选择要批量处理的告警");   await Promise.allSettled(state.selectedAlarmIds.map((id) => handleAlarmAction(action, id)));   state.selectedAlarmIds = [];   await loadAlarmRows(state.alarmsPage.page);   await loadScreenData();   await loadAnalysisData(); } async function handleAlarmAction(action, id) {   if (action === "confirm") await apiRequest("/alarms/" + id + "/confirm", { method: "PUT", body: JSON.stringify({ confirmMethod: "MANUAL" }) });   else if (action === "resolve") await apiRequest("/alarms/" + id + "/resolve", { method: "PUT", body: JSON.stringify({ resolveMethod: "ON_SITE", resolveDetail: "由前端快速处置" }) });   else if (action === "archive") await apiRequest("/alarms/" + id + "/archive", { method: "PUT" });   else if (action === "close") await apiRequest("/alarms/" + id + "/close", { method: "PUT", body: JSON.stringify({ remark: "由前端关闭" }) }); }  function appendChat(role, text) {   const log = el("chatLog");   if (!log) return;   const bubble = document.createElement("div");   bubble.className = "bubble " + (role === "user" ? "user" : "ai");   bubble.textContent = text;   log.appendChild(bubble);   log.scrollTop = log.scrollHeight; } function renderAiJudgement() {   const target = el("aiJudgement");   if (!target) return;   const active = getActiveAlarm();   if (!active) { target.textContent = "暂无活跃告警，请等待新的烟感事件。"; return; }   target.textContent = "当前存在告警：" + safeText(active.alarmType, "告警") + "，等级 " + safeText(active.alarmLevel, "--") + "，状态 " + safeText(active.alarmStatus, "--") + "，设备 " + safeText(active.deviceId, "--") + "，位置 " + safeText(active.locationBuilding || active.building, "未知楼栋") + "。建议先确认现场，再下发广播。"; } async function sendQuestion() {   const input = el("chatInput");   if (!input) return;   const question = input.value.trim();   if (!question) return;   if (!state.aiSessionId) state.aiSessionId = buildSessionId();   const active = getActiveAlarm();   appendChat("user", question);   input.value = "";   try {     const response = await apiRequest("/conversations", { method: "POST", body: JSON.stringify({ sessionId: state.aiSessionId, alarmId: active && active.id ? active.id : undefined, question }) });     const answer = safeText(response && (response.answer || response.content || response.reply), "暂无回复");     appendChat("ai", answer);     const judgement = el("aiJudgement");     if (judgement) judgement.textContent = answer;   } catch (error) {     appendChat("ai", "调用失败: " + error.message);   } } async function sendBroadcast() {   const active = getActiveAlarm();   const selectedDevice = (state.devicesPage.records || []).find((item) => String(item.id) === String(state.selectedDeviceId));   if (!active || !active.id) { showGlobalAlert("当前没有可广播的活跃告警"); return; }   const deviceId = active.deviceId || (selectedDevice && selectedDevice.id);   if (!deviceId) { showGlobalAlert("请先选择设备或等待告警关联设备加载完成"); return; }   const area = safeText(active.locationBuilding || active.building || (selectedDevice && (selectedDevice.locationBuilding || selectedDevice.building)), "当前区域");   try {     await apiRequest("/broadcasts", { method: "POST", body: JSON.stringify({ alarmId: active.id, deviceId, broadcastArea: area, broadcastContent: "【紧急疏散】检测到告警，请相关区域人员立即按照消防预案有序撤离。", broadcastType: "EMERGENCY", triggerMode: "MANUAL" }) });     showGlobalAlert("广播指令已下发");     await loadScreenData();   } catch (error) {     showGlobalAlert("广播失败: " + error.message);   } } function showGlobalAlert(text) {   const node = el("globalAlert");   if (!node) return;   node.textContent = text;   node.classList.remove("hidden");   clearTimeout(showGlobalAlert.timer);   showGlobalAlert.timer = setTimeout(() => node.classList.add("hidden"), 4000); }  async function loadHealthStatus() {   try {     const health = await apiRequest("/health");     const status = String(health.status || "UNKNOWN").toUpperCase();     setChip("healthStatus", "服务: " + status, status === "UP" ? "ok" : "danger");     const components = health.components || {};     setChip("mqttStatus", "MQTT: " + safeText(components.mqtt, "--"), String(components.mqtt || "").toUpperCase() === "UP" ? "ok" : "warn");     setChip("redisStatus", "Redis: " + safeText(components.redis, "--"), String(components.redis || "").toUpperCase() === "UP" ? "ok" : "warn");   } catch (error) {     setChip("healthStatus", "服务: 获取失败", "danger");     setChip("mqttStatus", "MQTT: --", "warn");     setChip("redisStatus", "Redis: --", "warn");   } } async function loadScreenData() {   try {     const [stats, realtime, alarmPage] = await Promise.all([       apiRequest("/dashboard/stats"),       apiRequest("/dashboard/realtime?count=24"),       apiRequest("/alarms?page=1&pageSize=50"),     ]);     state.screen.stats = stats || {};     state.screen.realtime = realtime || {};     state.screen.alarmSample = normalizePageResult(alarmPage, 1, 50).records;     renderScreenKpi();     renderLatestMetrics();     renderScreenAlarmList();     renderScreenCharts();     renderAiJudgement();     setSyncTime();   } catch (error) {     console.error(error);     showGlobalAlert("大屏数据加载失败: " + error.message);   } } async function loadAnalysisData() {   try {     const [alarmTrend, deviceStats, alarmPage] = await Promise.all([       apiRequest("/dashboard/alarm-stats?period=7"),       apiRequest("/dashboard/device-stats"),       apiRequest("/alarms?page=1&pageSize=100"),     ]);     state.analysis.alarmTrend = Array.isArray(alarmTrend) ? alarmTrend : [];     state.analysis.deviceStats = Array.isArray(deviceStats) ? deviceStats : [];     state.analysis.alarmSample = normalizePageResult(alarmPage, 1, 100).records;     renderAnalysisCharts();   } catch (error) {     console.error(error);     showGlobalAlert("数据分析加载失败: " + error.message);   } } async function loadDevices(page = state.devicesPage.page || 1) {   const keyword = safeText(el("deviceKeyword") && el("deviceKeyword").value, "").trim();   const status = safeText(el("deviceStatusFilter") && el("deviceStatusFilter").value, "").trim();   let query = "?page=" + page + "&pageSize=" + state.devicesPage.pageSize;   if (keyword) query += "&keyword=" + encodeURIComponent(keyword);   if (status) query += "&status=" + encodeURIComponent(status);   try {     const data = await apiRequest("/devices" + query);     state.devicesPage = normalizePageResult(data, page, state.devicesPage.pageSize);     renderDevicesTable();     renderDevicePagination();     updateScreenDeviceSelect();   } catch (error) {     console.error(error);     showGlobalAlert("设备数据加载失败: " + error.message);   } } async function loadAlarmRows(page = state.alarmsPage.page || 1) {   const status = safeText(el("alarmStatusFilter") && el("alarmStatusFilter").value, "").trim();   let query = "?page=" + page + "&pageSize=" + state.alarmsPage.pageSize;   if (status) query += "&status=" + encodeURIComponent(status);   try {     const data = await apiRequest("/alarms" + query);     state.alarmsPage = normalizePageResult(data, page, state.alarmsPage.pageSize);     renderAlarmTable();     renderAlarmPagination();   } catch (error) {     showGlobalAlert("告警数据加载失败: " + error.message);   } } function connectWebSocket() {   try {     if (!location.host) { setChip("wsStatus", "WebSocket: 已断开", "warn"); return; }     const wsUrl = (location.protocol === "https:" ? "wss://" : "ws://") + location.host + "/ws/alarm";     const socket = new WebSocket(wsUrl);     socket.onopen = () => setChip("wsStatus", "WebSocket: 已连接", "ok");     socket.onclose = () => setChip("wsStatus", "WebSocket: 已断开", "warn");     socket.onmessage = (event) => showGlobalAlert("实时告警: " + event.data);   } catch (error) {     setChip("wsStatus", "WebSocket: 不可用", "warn");   } }  function bindEvents() {   const screenRefresh = el("btnRefreshScreen");   const analysisRefresh = el("btnRefreshAnalysis");   const devicesRefresh = el("btnRefreshDevices");   const aiRefresh = el("btnRefreshAi");   const alarmsRefresh = el("btnRefreshAlarms");   const btnSearchDevices = el("btnSearchDevices");   const btnLoadAlarms = el("btnLoadAlarms");   const btnSendQuestion = el("btnSendQuestion");   const btnBroadcast = el("btnBroadcast");   const btnSelectAllDevices = el("btnSelectAllDevices");   const btnClearDevices = el("btnClearDevices");   const btnBatchDeleteDevices = el("btnBatchDeleteDevices");   const btnSelectAllAlarms = el("btnSelectAllAlarms");   const btnClearAlarms = el("btnClearAlarms");   const btnBatchConfirmAlarms = el("btnBatchConfirmAlarms");   const btnBatchResolveAlarms = el("btnBatchResolveAlarms");   const btnBatchArchiveAlarms = el("btnBatchArchiveAlarms");   const btnBatchCloseAlarms = el("btnBatchCloseAlarms");   const deviceSelectAll = el("deviceSelectAll");   const alarmSelectAll = el("alarmSelectAll");   const screenDeviceSelect = el("screenDeviceSelect");   const chatInput = el("chatInput");   const btnLogout = el("btnLogout");    if (screenRefresh) screenRefresh.addEventListener("click", async () => { await loadScreenData(); await loadDevices(); });   if (analysisRefresh) analysisRefresh.addEventListener("click", loadAnalysisData);   if (devicesRefresh) devicesRefresh.addEventListener("click", () => loadDevices(1));   if (aiRefresh) aiRefresh.addEventListener("click", async () => { await loadHealthStatus(); await loadScreenData(); });   if (alarmsRefresh) alarmsRefresh.addEventListener("click", () => loadAlarmRows(1));   if (btnSearchDevices) btnSearchDevices.addEventListener("click", () => loadDevices(1));   if (btnLoadAlarms) btnLoadAlarms.addEventListener("click", () => loadAlarmRows(1));   if (btnSendQuestion) btnSendQuestion.addEventListener("click", sendQuestion);   if (btnBroadcast) btnBroadcast.addEventListener("click", sendBroadcast);   if (btnSelectAllDevices) btnSelectAllDevices.addEventListener("click", () => { state.selectedDeviceIds = (state.devicesPage.records || []).map((item) => String(item.id)); renderDevicesTable(); });   if (btnClearDevices) btnClearDevices.addEventListener("click", () => { state.selectedDeviceIds = []; renderDevicesTable(); });   if (btnBatchDeleteDevices) btnBatchDeleteDevices.addEventListener("click", batchDeleteDevices);   if (btnSelectAllAlarms) btnSelectAllAlarms.addEventListener("click", () => { state.selectedAlarmIds = (state.alarmsPage.records || []).map((item) => String(item.id)); renderAlarmTable(); });   if (btnClearAlarms) btnClearAlarms.addEventListener("click", () => { state.selectedAlarmIds = []; renderAlarmTable(); });   if (btnBatchConfirmAlarms) btnBatchConfirmAlarms.addEventListener("click", () => batchHandleAlarms("confirm"));   if (btnBatchResolveAlarms) btnBatchResolveAlarms.addEventListener("click", () => batchHandleAlarms("resolve"));   if (btnBatchArchiveAlarms) btnBatchArchiveAlarms.addEventListener("click", () => batchHandleAlarms("archive"));   if (btnBatchCloseAlarms) btnBatchCloseAlarms.addEventListener("click", () => batchHandleAlarms("close"));   if (deviceSelectAll) deviceSelectAll.addEventListener("change", () => { state.selectedDeviceIds = deviceSelectAll.checked ? (state.devicesPage.records || []).map((item) => String(item.id)) : []; renderDevicesTable(); });   if (alarmSelectAll) alarmSelectAll.addEventListener("change", () => { state.selectedAlarmIds = alarmSelectAll.checked ? (state.alarmsPage.records || []).map((item) => String(item.id)) : []; renderAlarmTable(); });   if (chatInput) chatInput.addEventListener("keydown", (event) => { if (event.key === "Enter") sendQuestion(); });   if (screenDeviceSelect) screenDeviceSelect.addEventListener("change", () => { state.selectedDeviceId = screenDeviceSelect.value; });   if (btnLogout) btnLogout.addEventListener("click", handleLogout);   document.addEventListener("click", (event) => {     const modal = el("detailModal");     if (modal && !modal.classList.contains("hidden") && event.target === modal.querySelector(".modal-mask")) closeDetailModal();   }); }  async function bootstrap() { renderUserInfo();   state.aiSessionId = buildSessionId();   initMenus();   bindEvents();   setClock();   setInterval(setClock, 1000);   connectWebSocket();   await loadHealthStatus();   await Promise.all([loadDevices(1), loadScreenData(), loadAnalysisData(), loadAlarmRows(1)]);   renderAiJudgement();   setInterval(async () => { await loadHealthStatus(); await loadScreenData(); }, 20000); }  bootstrap();